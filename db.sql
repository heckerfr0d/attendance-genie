CREATE TABLE users (id SERIAL PRIMARY KEY, username VARCHAR(10) UNIQUE NOT NULL, password VARCHAR(32) NOT NULL, disco VARCHAR(19));
CREATE TABLE schedule (id INTEGER PRIMARY KEY, uid INTEGER REFERENCES users(id) NOT NULL, time TIME WITH TIME ZONE NOT NULL, link VARCHAR(6) NOT NULL, marked BOOLEAN, tries INTEGER NOT NULL);